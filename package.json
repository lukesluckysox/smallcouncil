{
  "name": "small-council",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:init": "node -e \"require('dotenv').config(); const { execSync } = require('child_process'); execSync('psql ' + process.env.DATABASE_URL + ' -f sql/schema.sql', { stdio: 'inherit' });\"",
    "db:init:direct": "psql $DATABASE_URL -f sql/schema.sql"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.8.0",
    "next": "^14.2.35",
    "react": "^18",
    "react-dom": "^18",
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/uuid": "^9.0.8",
    "eslint": "^8",
    "eslint-config-next": "^14.2.35",
    "typescript": "^5"
  }
}
